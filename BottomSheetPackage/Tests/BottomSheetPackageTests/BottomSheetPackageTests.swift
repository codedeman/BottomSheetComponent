import XCTest
@testable import BottomSheetPackage

final class BottomSheetPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BottomSheetPackage().text, "Hello, World!")
    }
}
